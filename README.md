# Basic Node Executing Graph App

To prepare Graph App for installation in Neo4j Desktop:

```
npm run build
npm pack
```

This will generate a .tgz file that can then be dragged into the Neo4j Desktop install Graph App window
