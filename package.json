{
  "name": "basic-node-executor",
  "description": "Graph App - Basic Node Executor",
  "homepage": ".",
  "version": "1.0.0",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && mkdir dist && cp index.html nodeScript.js manifest.json dist",
    "serve-dist": "serve -s dist"
  },
  "neo4jDesktop": {
    "apiVersion": "1.3.0"
  },
  "devDependencies": {
    "serve": "^11.3.0"
  }
}
